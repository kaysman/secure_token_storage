name: secure_token_storage_example
description: Demonstrates SecureTokenStorage with a fake login flow.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  secure_token_storage:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
